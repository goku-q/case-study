1、volatile介绍
volatile关键字，是java提供的轻量级的同步机制。
特点：
保证了可见性：被volatile修饰的变量，在修改后会立即将修改后的值存入主存，从而被别的线程发现值已修改
不保证原子性：在多线程的情况下无法保证原子性，在多线程同时执行时，执行速度极快，不能保证修改后立即被发现
禁止指令重排：java的代码顺序在JVM中，编译器或CPU会自动优化进行指令重排，存在数据依赖关系的指令不会重排，但是多线程的环境下无依赖的数据就无法保证变量的一致性。

